package com.legalmetrology.inspector.ui.theme

import androidx.compose.ui.graphics.Color

// ============================================================
// LEGAL METROLOGY APP — PREMIUM LIGHT DESIGN SYSTEM
// Palette: Executive indigo/slate primary, warm off-whites,
//          refined success/warning/error accents
// Editorial fintech-inspired, clean government inspection aesthetic
// ============================================================

// --- Brand / Primary (Executive Slate/Indigo) ---
val Slate950    = Color(0xFF0F172A)      // Deep executive primary
val Slate900    = Color(0xFF1E293B)      // Primary dark variant
val Slate800    = Color(0xFF334155)
val Slate700    = Color(0xFF475569)
val Slate600    = Color(0xFF64748B)
val Slate500    = Color(0xFF94A3B8)
val Slate400    = Color(0xFFCBD5E1)
val Slate300    = Color(0xFFE2E8F0)      // Crisp borders
val Slate200    = Color(0xFFF1F5F9)      // Subtle surface
val Slate100    = Color(0xFFF8FAFC)      // Off-white background
val Slate50     = Color(0xFFFCFDFE)      // Lightest surface

// --- Refined Cobalt Accent (optional interactive highlight) ---
val Cobalt600   = Color(0xFF2563EB)      // Refined blue for CTAs
val Cobalt500   = Color(0xFF3B82F6)
val Cobalt100   = Color(0xFFDBEAFE)

// --- Status Colors (Refined for light mode) ---
val Emerald600  = Color(0xFF059669)      // Pass / Compliance
val Emerald500  = Color(0xFF10B981)
val Emerald100  = Color(0xFFD1FAE5)
val Emerald50   = Color(0xFFECFDF5)

val Amber600    = Color(0xFFD97706)      // Warning / Pending
val Amber500    = Color(0xFFF59E0B)
val Amber100    = Color(0xFFFEF3C7)
val Amber50     = Color(0xFFFFFBEB)

val Crimson600  = Color(0xFFDC2626)      // Fail / Violation
val Crimson500  = Color(0xFFEF4444)
val Crimson100  = Color(0xFFFEE2E2)
val Crimson50   = Color(0xFFFEF2F2)

val Blue600     = Color(0xFF2563EB)      // Info / Inconclusive
val Blue500     = Color(0xFF3B82F6)
val Blue100     = Color(0xFFDBEAFE)
val Blue50      = Color(0xFFEFF6FF)

// --- Text (Light Mode) ---
val TextPrimary     = Color(0xFF0F172A)  // Slate 950 for high contrast
val TextSecondary   = Color(0xFF475569)  // Slate 700 for muted text
val TextTertiary    = Color(0xFF94A3B8)  // Slate 500 for disabled/hint

// --- AR Overlay (adapted for light theme camera overlay) ---
val ArReticleTint   = Color(0xFF059669)  // Emerald for locked state
val ArSearchTint    = Color(0xFF3B82F6)  // Cobalt for searching
val ArGlassPanel    = Color(0xCCF8FAFC)  // 80% opacity light slate for glass panels
